{
  "name": "eki-ecosystem",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source GPS tracking MVP using ESP32 GNSS telemetry, streaming to Firebase RTDB with a Next.js routing engine.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build": "npm run build --workspace=frontend",
    "deploy": "npm run build && npx firebase deploy",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
